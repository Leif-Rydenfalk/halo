"""mate.py — the joint, as a transform.

Contract (TRIAD.md): `def mate(a_iface, b_iface, params=None) -> Mate` —
the transform, the DOF the joint leaves free, and the fastener BOM it adds.
"""


def mate(a_iface, b_iface, params=None):
    raise NotImplementedError("bayonet-3-tab: no mate yet")
